# Theming

Customize the look & feel using CSS variables in `app/globals.css`.

## Colors

Override the `:root` and `.dark` variables for brand colors.

```css
:root {
  --primary: 220 90% 56%;
}
```

## Typography

Use Tailwind font utilities on your components, or adjust the font import in `app/layout.tsx`.
