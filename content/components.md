# Components

This boilerplate ships with a small set of UI primitives.

## Button

```tsx
import { Button } from '@/components/ui/button'

<Button>Click me</Button>
```

## Sidebar

The sidebar navigation lives in `components/ui/sidebar.tsx` and is wired in `app/layout.tsx`.
